# 2026-06-01 工作日志

- 修复 `utils.py` 的 `http_get_text` SSL 问题：改用 `requests.get()` 优先（自带 certifi CA 证书），`urllib` 作为 fallback；修复后 Treasury FiscalData API（`api.fiscaldata.treasury.gov`）可正常访问
- 重新执行完整流水线：`scripts/run.py` 成功抓取 33 metrics + 18 derived signals + upcoming_auctions（TreasuryDirect 未来60天拍卖日程）
- 模型自主生成 `analysis.json`，stance = **中性偏松**，置信度中等
- 核心数据：SOFR-IORB = -3.0bp（温和偏松）；RRP = 11.68bn（+10.51bn，边际回升但绝对极薄）；WRESBAL = 3,066.56bn（周 -63bn）；TGA = 849.71bn（+7.05bn）；10Y real yield = 2.06%（-3bp）
- 未来 T-bill 供给：2026-06-01 有 $166bn（13-Week $89bn + 26-Week $77bn），2026-06-02 有 $75bn（6-Week）；合计 $241bn 本周集中发行，需关注今日拍卖结果
- T-bill 拍卖吸收：TBILL_AUCTION_ABSORPTION = 251.0bn，BTC 2.86x（前期 2.90x），小幅走弱但仍健康
- 前端已在 localhost:8080 正常预览
