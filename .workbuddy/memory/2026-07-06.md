# 2026-07-06

## 更新数据并分析（完整流水线）

- 运行 `scripts/run.py "今天的美元流动性简报" --json`：抓取最新 snapshot（核心日频截至 2026-07-02；IORB 政策锚截至 2026-07-06；DTWEXBGS/NFCI/CP_PROXY 滞后至 2026-06-26，REPO_FAILS_UST 滞后至 2026-06-17）。清理旧时间戳文件 8 个。规则分 1.9 → 中性。
- 模型（AI agent）自主读取 `model_input.json` + `prompts/usd_liquidity_prompt.md`，生成结构化 `analysis.json`：stance=中性（信心中等），4 条 key_takeaways + 4 条 risk_flags。
- 关键风险信号：P1 RRP 缓冲垫接近耗尽（RRPONTSYD 2.175bn，绝对水平极低）；P1 日元 carry 平仓风险中性偏高（CFTC 日元净空 -33.9%、美日 10Y 利差 179bp）；P2 data 多指标滞后；P2 长端名义收益率温和上行（DGS10 4.48% +4bp）。
- 本地 HTTP server 已启动（127.0.0.1:8765），前端 `frontend/usd-liquidity-monitor/index.html` 经 loadJson 自动 fetch `analysis.json`，HTTP 200 验证通过。
- 已提交并 push 到 GitHub（commit 083c2c7，origin/main 同步，0 ahead/0 behind）。提交范围：output/latest 全部 JSON + frontend data.js + 记忆文件。

## JPY carry CFTC 净空头的口径澄清（用户反馈）

- 用户指出：CFTC 日元净空头边际变多 = 投机盘卖出日元、买入美元，对美元是短期利好（资金流支撑轴）。之前分析只强调极值水平的未来踩踏，重心偏颇。
- 修正：JPY carry 信号现明确分两层——(1) 当下流量支撑：边际建仓持续卖出日元买入美元，对 USD/JPY 与美元风险资产直接利好；(2) 前瞻脆弱性：极值水平使未来反转踩踏更剧烈。两层并存不矛盾（不同时间轴）。
- 已更新 analysis.json 的 jpy_carry one_liner/analysis 与 P1 风险信号 text/evidence，commit 63ea785，已 push。
- 待办（用户未确认）：是否把"CFTC 净仓位必须拆水平+边际"固化进 prompts/usd_liquidity_prompt.md 与 scripts/analysis.py 的 prompt 注入，使每次流水线自动带这层。

## 流水线改造：CFTC 拆多空分项 + short_share 代理（用户深挖）

- 用户四个追问：(1) 有抓边际持仓量吗？(2) 有分析边际吗？(3) 提高这部分分析重要性；(4) 净空头变多可能因多头平仓，需"证明空头真加仓"的代理变量（如持仓比例）。
- 排查结论：fetch_cftc_jpy 原本就从 CFTC API 抓了 noncomm long/short/open_interest，但只存 net_oi，多头/空头/持仓量算完即弃；prompt 与 analysis.py 注入均未要求拆解多空与边际。手动补的边际分析不进流水线。
- 改造（commit 9218122，已 push）：
  - scripts/jpy_carry.py：fetch_cftc_jpy 增加 short_share=空头/(空头+多头)；main() 计算 gross short/long/OI 序列、周度边际、net 变化归因 _decompose_cftc_net（driver: short_building / long_unwinding / mixed / unknown）；持久化 CFTC_JPY_GROSS_SHORT/LONG/SHORT_SHARE/OPEN_INTEREST 到 DB 与 history；新增卡片、两张图表、cftc_decomposition 字段。
  - prompts/usd_liquidity_prompt.md + scripts/analysis.py jpy_carry_rule：CFTC 仓位强制水平+流量两层、必须拆分多空、short_share 为"空头真加仓"代理、仅 short_building 时可下"carry 支撑美元"结论、提升权重。
- 实测打脸旧叙事：最新周 gross 空头 259,802 口、周变化 −7,705（减仓），gross 多头 −3,677，driver=long_unwinding；净空头变多只是多头平仓、空头亦减仓，并非新空头建仓。故"空头仍在加仓=当前美元支撑"误读已纠正→ JPY carry 风险重定为"水平极值(short_share 69.6%、近1年100分位)但边际退潮，风险在存量而非增量"。
- 端到端重跑 run.py + 模型重写 analysis.json，前端 loadJson 已反映。验证：analysis.json schema 通过，origin/main 同步 0/0。
