# 2026-07-19 美元流动性简报

## 流水线执行

- `scripts/run.py` 成功运行：43 metrics + 24 derived signals + 5 upcoming auction details
- 模型自主生成 `output/latest/analysis.json`，总判断：**中性偏松**
- 前端预览已启动，Git push 到 origin/main 成功

## 核心数据

- SOFR 3.62% < IORB 3.65%（-3bp），回购融资宽松
- TGA $822.51bn（单日 +$26.5bn），RRP $0.1bn 缓冲垫耗尽
- 国债收益率：1Y 4.01%(+2bp) / 3Y 4.21%(+1bp) / 5Y 4.28%(0) / 7Y 4.40%(-1bp) / 10Y 4.55%(-2bp)，曲线平坦化
- IG OAS 0.78%(-1bp)，HY OAS 2.71%(0)，VIX 16.73(+1.06)
- T-bill BTC 2.84x，压力评分 20（降）
- JPY carry: CFTC short_building，空头占比 67.3%（88th%ile），USD/JPY 162.28

## 风险

- P1: RRP 缓冲垫耗尽、TGA 快速累积趋势
- P2: VIX 微升、CP_PROXY/DTWEXBGS/JGB 数据滞后严重、carry 踩踏隐患

## JGB 10Y 陈旧数据修复（本次会话第二项任务）

- 根因：MOF `jgbcme_all.csv` 长期停滞在 6/30，导致 JGB10 卡与美日10Y利差 as_of 停在 6/30（19天陈旧）；CNBC 实时债券报价可到 7/18 但 JP10Y 在流水线快速连续请求下间歇性失败（code 1 / 无 last）。
- 修复：`scripts/jpy_carry.py` 的 `fetch_cnbc_jgb()` 重写为稳健版——各期限单符号请求、请求间 0.5s 间隔、每期限 5 次重试 + 退避；首轮缺漏期限再走专属 recovery pass（1.5s 间隔、6 次重试）。`build_payload` 在 CNBC 部分期限缺失时向 `notes` 写入告警（不再静默部分失败）。
- 验证：重新跑 `jpy_carry.py` 后 JGB10 卡 as_of=2026-07-18（2.692%），SQLite `jpy_carry_series_ts` 中 JGB2/5/10/30 全部写入 7/18 补丁；US_JP_10Y 利差从 6/30 推进到 7/16（受 DGS10 最新交易日限制，不再是日债端陈旧拖累）。
- 随后重跑 `run.py`：dashboard_data 日债端新鲜；模型自主生成 analysis.json（总判断 **中性**，含 RRP 缓冲耗尽 P1、CFTC 空头拥挤 P2、数据滞后 P2-data）。
- 属数据源修复（非口径改动），无需同步 prompt/analysis.py 注入规则（规则 15b 不触发）。

## 月频数据全面替换为日频（本次会话第三/四项任务）

- 触发：用户先问"还有别的类似陈旧问题吗一起修"，再明确"哪些数据是月维度更新的？这种对我们没意义，替换成日维度更新的"；AskUserQuestion 选定「NEER 换 BIS 日频，REER 删除」。
- 频率全量扫描结论：整条流水线里**唯一真正月频**的序列就是 JPY_NEER / JPY_REER（BOJ FM09 有效汇率，滞后约1.5月，之前卡在 2025-01/2026-05）。其余全部日频/T+1/周频/事件驱动，无需动。
- 修复（`scripts/jpy_carry.py`）：
  - 新增 `fetch_bis_neer()`：拉 BIS Data Portal SDMX 日频 NEER，key=`D.N.B.JP`（Daily/Nominal/Broad/Japan），url `https://stats.bis.org/api/v2/data/dataflow/BIS/WS_EER/1.0/D.N.B.JP?format=csv`；节假日行为 NaN 需过滤；latest 2026-07-14=67.73，仅滞后数个工作日。
  - **REER 彻底删除**：真实有效汇率需月频 CPI，任何源都没有日频 REER。删除 REER card / series / chart series / persist 项；DB 加一次性 `DELETE ... WHERE series_id='JPY_REER'`。
  - NEER 卡改日频口径：`逐日 {chg1:+.2f}｜约1月 {chg20:+.2f}`；陈旧阈值由 >120 天（月频）改为 >10 天（日频）。
  - 图表 id 保留 `jpy_effective_fx_3y`（避免 dashboard.py `build_jpy_carry_overlay` 与 app.js chart_ids 引用断裂），仅从双序列 NEER+REER 改为单序列 NEER，title 改「BIS日频」。
  - sources/persist/meta lookback 文案同步；`start_monthly`/`fmt_asof_month` 变为无用残留但无害。
- 验证：`jpy_carry.py`（risk 中性偏高 2.0）+ `run.py`（stance 中性 0.6）均成功；DB 中 JPY_NEER 900 行 source=BIS latest 7/14，JPY_REER count=0；dashboard_data JPY 卡 ids 无 JPY_REER（此前脚本里 "REER" 命中仅来自 trigger 文案，误报）。
- 模型自主生成 `output/latest/analysis.json`（stance 中性，3 takeaways + 3 risk flags），P2-data 条目显式记录「NEER 已切 BIS 日频、REER 删除、DCPN3M/REPO_FAILS 属源滞后非 bug」。
- 属数据源升级（NEER 换源、REER 删除），非分析口径改动，规则 15b 不触发。
