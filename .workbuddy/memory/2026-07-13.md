# 2026-07-13 工作记录

## 更新数据并分析（完整流水线）

- 运行 `python3 scripts/run.py "更新数据并分析" --json`：抓取 33 个原始指标 + 22 个衍生信号，RRP/SOMA/WRESBAL/TGA 落库 SQLite，图表生成 7d/30d 两张 SVG，并重置 `analysis.json` 为 `pending_model_analysis` 占位符。
- 模型（AI agent）读取 `output/latest/model_input.json` + `prompts/usd_liquidity_prompt.md`，生成结构化 `analysis.json`（同时写入 `output/latest/analysis.json` 与 `output/usd_liquidity_analysis_20260713_102658.json`）。
- 本次总判断：**中性**（置信度中等）。规则基线约 -0.7。
  - 宽松侧：SOFR 3.53% 低于 IORB 12bp（成交 3,126bn，SOFR_VOLUME_IMPACT -10.4mn/day）；全曲线收益率小幅下行（1Y -4bp/3Y -3bp/10Y -2bp）；VIX 15.84 低位；IG/HY OAS 持平。
  - 收紧侧（结构性）：**RRP 缓冲垫近乎耗尽**——RRP 余额约 0.55tn（2026-07-10），前一日因临时因素冲高至约 5.77tn 后完全回吐；边际流量 -5.2tn 短期释放流动性，但缓冲垫枯竭令未来 TGA 补库/QT/美债供给冲击直接打到准备金。DB 历史印证 RRP 近月剧烈波动（06-30 季末 26.9tn → 07-10 0.55tn）。
  - 日元 carry：仍顺风（USD/JPY 162.35、JPY 成本 0.978%、US-JP 10Y 利差 185bp），但 CFTC 空头占比 67.8%（近1年90分位）拥挤；driver=mixed（空头减仓30,939口、多头仅增375口）→ 属"拥挤待平"非"正在加杠杆"，不得下"空头加仓支撑美元"结论。
  - 数据滞后风险：DTWEXBGS(07-02)、NFCI(07-03) 滞后约1–2周，仅作背景。
- 前端：本地 `python -m http.server 8765` 已起，访问 `frontend/usd-liquidity-monitor/index.html`；确认 `app.js` 的 loadJson 占位符修复生效（检测到 pending 会 fetch 真实 analysis.json）。
- 已 git add/commit/push `output/latest/*`、`frontend/usd-liquidity-monitor/data.js` 及本记忆文件到 origin/main。

## 备注
- 本次 RRP 取值表注入单位标注为 "bn" 但实际是脚本内部万亿单位（0.545/5.772），输出分析时按经济真实量级（约0.55tn/5.77tn）表述，避免误导读者。
- 铁律复查：`analysis.py` 注入字符串与 `prompts/usd_liquidity_prompt.md` 口径一致（RRP_FLOW/RRP_BUFFER 两层、10Y 用 DGS10 名义、JPY driver=mixed 不称空头加仓），本次未改口径，仅按数据生成分析。

## 数据新鲜度排查 + 更快源改造（用户质疑"数据是否真最新"）
- 实测各指标真实 as_of：日频利率/收益率/SOFR/TGA = 7-09（周四），RRP=7-10（周五，NY Fed 同日源），WRESBAL/SOMA 周频=7-08，滞后项 DCPN3M=6-26(17d)、DTWEXBGS=7-02(11d)、REPO_FAILS_UST=7-01(12d)、NFCI=7-03(10d)；仅 IORB（政策）显示 7-13。
- 根因：上次触发在 GMT+8 周一 10:27 = 美东周日 22:27，美国周末休市，最新交易日为周五 7-10；FRED 日频序列（收益率/SOFR/TGA）需周一美东早间才发布 7-10 数据 → 当时只拿到 7-09。RRP 走 NY Fed API（同日 1:15pm ET 发布）已是最新。即当时数据已是物理可达最新，问题是**触发时间选在美周末**。
- 更快源改造（已实现并单测通过）：收益率抓取源从 FRED(T+1) 改为 **Treasury.gov 每日国债收益率曲线**（同日 16:00 ET 发布）。新增 `fetch_treasury_par_yield_curve()` 于 `scripts/fetchers.py`，返回 DGS1/DGS2/DGS3/DGS10/DGS30/DGS3MO + 计算 T10Y2Y/T10Y3M，id 不变、FRED 作兜底；在 `fetch_all_metrics` 中于 fred_jobs 之后 extend（稳定排序保证同日源覆盖 FRED）。实测拿到 7-10 同日值（DGS10=4.56、10Y-2Y=0.35、10Y-3M=0.71），stale 4→3。
- SOFR/EFFR/TGA/WRESBAL 结构性 T+1 或周频，无公开实时等价源；DCPN3M/DTWEXBGS/REPO_FAILS/NFCI 为周频或提供商滞后，只能作背景、无法靠换源提速。
- **关键建议（尚未实施）**：因北京时间比美东早 12h，应在**美股收盘+数据发布后**巡检才有同日数据。最优窗口 ≈ 北京时间 21:00（周一至周五）：此时昨日 Treasury 同日收益率(4am)、RRP(NY Fed 1:15am)、FRED 的 SOFR/EFFR/TGA(约 8:30pm 美东=8:30pm 北京) 全部齐备。待用户确认后以 automation 固定每日 21:00 北京时间运行 `run.py`。

## 前端预览环境约束（重要）
- 本环境会回收后台进程（run_in_background 的 http.server 也会在几分钟内被回收），本地 localhost 预览经代理返回 502，用户无法访问。
- 解决方案：模型写完 analysis.json 后，应额外把真实分析内联进 `frontend/usd-liquidity-monitor/data.js`（覆盖 run.py 写入的 `pending_model_analysis` 占位符），使 index.html 可经 file:// 直接打开；或生成完全自包含的离线 HTML（`output/latest/usd_liquidity_dashboard_standalone.html`，CSS+Chart.js+data+app.js 全部内联，零网络依赖）。
- 本次已生成离线版并刷新 data.js，用户可直接打开离线 HTML 查看仪表盘。
