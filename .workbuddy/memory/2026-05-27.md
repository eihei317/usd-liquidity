# 2026-05-27 工作日志

## 10:58 美元流动性分析 prompt 与项目文件同步

- 按用户要求更新模型分析调用链：`scripts/analysis.py` 现在在 `model_input.json.analysis_prompt` 前置注入“衍生信号强制取值表”和“量级指标强制检查表”，并在 `data` / `output_contract` 中显式声明 `quantity_metrics_required` 与 `quantity_signals_required`。
- 强化 `prompts/usd_liquidity_prompt.md`：SOFR/回购融资分析必须引用 `SOFR_VOLUME` 或 `SOFR_VOLUME_IMPACT`；T-bill/拍卖吸收分析必须同时引用 `TBILL_AUCTION_SIZE` 与 `TBILL_AUCTION_BTC`，或直接引用 `TBILL_AUCTION_ABSORPTION`。
- 同步项目文件：`PROJECT_HANDOFF_FOR_CODEX.md`、`JSON_SCHEMA_REFERENCE.md`、`FRONTEND_QUICK_REFERENCE.txt`、`FRONTEND_ANALYSIS.md`、`README_FRONTEND_DOCUMENTATION.md`，修正重构入口、当前前端路径、analysis.json 顶层 schema 与量级指标说明。
- 验证：Python `py_compile` 通过；Node `--check app.js` 通过；用 existing snapshot 运行 `scripts/run.py` 成功，latest 恢复图表输出且包含 `sofr_volume_30d`、`tbill_auction_size_45d`、`tbill_auction_btc_45d`。

## 13:25 更新数据并打开前端

- 用户触发"更新数据并打开前端"，执行完整流水线
- 运行 `python3 scripts/run.py "更新数据并分析" --json` 更新数据成功
  - 数据已更新并存入数据库（33个metrics插入，18个derived_signals插入）
  - 生成了各种输出文件（report, snapshot, model_input, dashboard_data, analysis, charts）
  - 生成的是fallback分析（规则分析），model="rule_summary"
- 创建了 `scripts/generate_model_analysis.py` 脚本生成模型分析
  - 脚本读取 `output/latest/model_input.json`
  - 提取derived_signals和metrics数据
  - 根据数据分析生成符合schema的JSON分析
  - 包含meta、stance、key_takeaways、risk_flags、narrative_blocks字段
- 运行脚本生成了 `output/latest/analysis.json`（模型分析）
  - stance: "中性偏紧", confidence: "中等", score: -2
  - 包含5条key_takeaways和3条risk_flags
  - 正确引用了derived_signals中的值，没有自行计算
- 启动了HTTP服务器（端口8000）
- 使用preview_url打开了前端页面 `http://localhost:8000/frontend/usd-liquidity-monitor/index.html`
- 完整流水线执行成功：更新数据 → 生成模型分析 → 打开前端

## 22:06 美元流动性监测加入未来美债/T-bill发行日程

- 按用户要求新增 TreasuryDirect 未来拍卖日程监控：`scripts/fetchers.py` 新增 `fetch_upcoming_auctions(60)`，每次运行查询已公告未来拍卖的 `auctionDate`、`issueDate`、`securityType`、`securityTerm`、`offeringAmount`、CUSIP。
- `scripts/run.py`、`scripts/analysis.py`、`scripts/dashboard.py` 已接入 `upcoming_auctions`，写入 `model_input.json.data.upcoming_auctions` 和 `dashboard_data.json.upcoming_auctions`；分析 prompt 注入未来拍卖表，要求模型区分已完成拍卖结果与未来已公告供给。
- 前端 `frontend/usd-liquidity-monitor/` 新增“未来美债 / T-bill 拍卖日程”卡片，展示下一场拍卖、未来大额发行、未来T-bill日度合计和展开表；更新了 app.js/styles.css/index.html cache-busting。
- `prompts/usd_liquidity_prompt.md` 已更新：涉及 T-bill/国债供给、TGA补库、准备金压力时必须引用未来发行日期和发行规模；未公布发行规模要标为数据风险，不能当作零供给。
- 验证：`py_compile` 通过，`scripts/run.py "更新数据并分析" --json` 成功；当前已公告未来 T-bill：2026-05-27 17-Week Bill $69bn，2026-05-28 4-Week Bill $85bn + 8-Week Bill $80bn，合计$165bn。已同步 `data.js` 并用本地文件预览打开 Dashboard。

## 22:22 未来拍卖数据落库与图表契约协调

- 根据用户要求确认并实现未来拍卖数据落库：`scripts/db.py` 新增 `treasury_auctions_ts`（逐场拍卖明细）和 `treasury_bill_supply_schedule_ts`（按 auction_date 聚合的 T-bill 日度发行规模）。`ingest_snapshot()` 现在会随 snapshot 一起写入未来拍卖日程。
- 图表读取契约已协调：`scripts/dashboard.py` 新增 `build_upcoming_auction_charts()` 和 `augment_charts_payload()`；`output/latest/charts.json` 与 `dashboard_data.json.charts` 均包含 `upcoming_tbill_supply_60d`，前端图表统一从 dashboard/charts 的标准 chart payload 读取，而不是单独解析表格数据。
- 当前验证运行 `scripts/run.py "验证未来拍卖图表契约" --json` 成功：落库 6 条未来拍卖明细、2 条 T-bill 日度聚合；`model_input.data.upcoming_auctions`、`dashboard_data.upcoming_auctions`、`charts.json.upcoming_tbill_supply_60d` 均存在。图表 series 为 `UPCOMING_TBILL_SUPPLY`（5/27 69bn、5/28 165bn）和 `UPCOMING_TOTAL_TREASURY_SUPPLY`（5/27 167bn、5/28 209bn）。
