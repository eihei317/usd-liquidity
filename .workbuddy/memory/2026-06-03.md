# 2026-06-03 工作记录

## 美元流动性监测 - 更新数据并分析

### 执行流程
1. 运行 `scripts/run.py "更新数据并分析" --json`
2. 发现 `requests` 模块缺失，安装 `requests pandas numpy`
3. 重跑脚本成功，生成 snapshot / model_input / dashboard_data / charts
4. 模型（WorkBuddy/internal）读取 `model_input.json` + `usd_liquidity_prompt.md`，生成结构化 `analysis.json` 并写入 `output/latest/analysis.json`
5. 启动 HTTP server（端口 3000），通过 `preview_url` 打开前端

### 数据新鲜度（截至 2026-06-02/01）
- RRP: $2.50bn（2026-06-02，日频，ok）—— 历史极低，缓冲垫接近耗尽
- 准备金 WRESBAL: $3,066.56bn（2026-05-27，周频，ok）—— 单周 -$63bn
- SOFR: 3.65%（2026-06-01，日频，ok）；SOFR VOLUME: 3,224bn
- T-bill 拍卖：$165bn（2026-05-28），BTC 2.92x（边际 -0.09x）
- 未来拍卖：2026-06-03 $69bn（17-Week）+ 2026-06-04 $150bn（4W+8W），合计 $219bn
- TGA: error（数据缺失，列为 P1 数据风险）
- SOMA: $6,324.46bn（2026-05-27，周频，ok）

### 本次分析结论
- stance: 中性偏紧
- 核心逻辑：RRP 极低 + 准备金持续流失 → 负债端脆弱性上升；未来两日 $219bn T-bill 发行是近期最直接压力测试
- 宽松对冲：IG/HY OAS 小幅改善，NFCI 仍为负值
- 数据风险：TGA 缺失，无法判断财政存款路径

### 踩坑
- `requirements.txt` 与实际代码不符（声称无外部依赖，但 `utils.py` import requests），需手动安装
- 后台 `python3 -m http.server` 启动后 `preview_url` 无法通过 localhost 访问（sandbox 网络限制），改用 `file://` 路径直接打开 HTML 成功
