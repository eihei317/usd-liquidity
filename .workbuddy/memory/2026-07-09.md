# 2026-07-09

## 美元流动性简报流水线运行

- 11:00 运行完整流水线：jpy_carry.py → run.py → 模型分析 → 前端预览 → git push
- 数据快照：snapshot 20260709_110141，metrics 32 条插入（1 跳过），derived 21 条
- TGA 数据缺失（FiscalData API 超时），标记为 data risk P2

### TGA 修复（11:11）
- 根因：`fetchers.py` 的 `fetch_tga()` 使用了默认 `timeout=10s`，FiscalData API 响应慢导致超时
- 修复：`http_get_json(url)` → `http_get_json(url, timeout=30, retries=2)`（与 TreasuryDirect/FRED 等长耗时接口对齐）
- 修复后 TGA 数据恢复：784.96bn（2026-07-07），prev 783.11bn，+$1.86bn 边际中性
- 重新分析：TGA 784.96bn + RRP 3.35bn 形成 Fed 负债端"双锁"格局，准备金缓冲空间压缩
- 移除 TGA 缺失的 data risk flag，DCPN3M（13天滞后）和 DTWEXBGS（7天滞后）保留为 P2 data risk

- 最终分析结果 `analysis.json`：总判断 **中性偏紧**（规则分数 0.60），置信度中等，已 push（044c48f）

### 核心发现
1. **国债收益率全线走高**：1Y +11bp → 4.06%（突破 4%），3Y +4bp → 4.18%，10Y +7bp → 4.55%，短端领涨，全曲线熊平
2. **RRP 缓冲垫仅 3.35bn**：存量接近耗尽，结构性脆弱持续（P1 风险）
3. **回购融资平静**：SOFR 3.62%（-1bp），SOFR-IORB -3bp，交易量 3,154bn
4. **信用端改善**：HY OAS -5bp 至 2.67%，IG OAS +1bp 基本持平，VIX 16.13 中性
5. **CFTC 日元空头拥挤度极端**：short_share 70.5%（近 1 年 100% 分位），driver = short_building

已提交并 push 到 GitHub（c13b77e）。
