# 2026-06-07 工作记录

## 美元流动性监测 - 完整流水线执行
- 11:01 运行 `scripts/run.py` 成功，33条指标入库，18条衍生信号更新
- 模型分析生成 `analysis.json`，stance: **中性偏紧**
- HTTP 服务（端口 8081）已启动，前端已打开

### 核心数据（2026-06-05/04）
| 指标 | 最新值 | 上期 | 边际变化 |
|------|---------|------|----------|
| **RRP** | **0.761bn** | 1.122bn | **-32.2%** |
| TGA | 844.5bn | 845.7bn | -1.2bn |
| 准备金 WRESBAL | 3,013.9bn | 3,066.6bn | -52.7bn |
| SOFR-IORB | -3bp | -4bp | +1bp（收窄） |
| T10Y-2Y | 38bp | 42bp | -4bp |
| T10Y-3M | 77bp | 69bp | +8bp |
| VIX | 15.4 | 16.06 | -0.66 |
| 10Y实际收益率 | 2.11% | 2.11% | 0%（T+2延迟） |

### 核心判断
- RRP暴跌至0.761bn（仅7.61亿美元），缓冲垫几乎耗尽
- SOFR-IORB利差收窄至-3bp，回购融资边际收紧
- Stance 从"中性偏松"调整为"中性偏紧"
- 核心风险：RRP耗尽后，任何流动性冲击将直接传导至准备金

---

## 前端大重构（v2）

### 12:12 用户要求调整前端

**改动1：统一判断区域**
- 去掉日元carry独立评分卡片
- 只保留一个"今日总判断"卡片在最上面
- 在 stance 卡片中加入日元Carry状态（从 `analysis.narrative_blocks.jpy_carry.label` 读取）

**改动2：合并关键变化+风险信号**
- 原来分成两个卡片（关键变化、风险信号）
- 现在合并为一个"关键信号"列表
- 每个信号有优先级标签（P0/P1/P2/INFO/NARR）
- 按优先级排序：P0 → P1 → P2 → INFO（takeaways）→ NARR（narrative_blocks）

**改动3：精简雷达区域**
- 日常交易雷达只显示 P0/P1 核心指标
- 二级确认和背景指标折叠显示

**改动4：图表统一30天**
- 所有图表改为最近一月视图
- 核心图表（展开显示）：
  1. `anchor_spreads_30d` - SOFR-IORB 利差
  2. `fed_liability_30d` - TGA + RRP
  3. `tbill_combined_supply_30d` - 合并美债发行量（新增）
  4. `us_jp_spread` - 美日利差
  5. `treasury_yields_30d` - 美债收益率 1Y/3Y/10Y
  6. `jpy_usdjpy_funding_1y` - JPY Carry
- 背景图表（折叠显示）：其他所有图表

**改动5：新增合并发行量图表**
- 新增 `tbill_combined_supply_30d` 图表
- 数据来源：历史 TBILL_AUCTION_SIZE（入库数据）+ 未来 upcoming_auctions.bill_schedule（已公告）
- 前20日历史 + 未来10日已公告

### 技术实现

**后端改动（`dashboard.py`）：**
1. `build_charts_payload()` 新增 `upcoming_auctions` 参数
2. 新增 `build_combined_treasury_supply_chart()` 函数，生成合并发行量图表
3. 新增 `us_jp_spread` 和 `jpy_usdjpy_funding_1y` 图表到 specs

**后端改动（`run.py`）：**
1. 调用 `build_charts_payload(series_bundle, upcoming_auctions)` 时传入 upcoming_auctions

**前端改动（`index.html`）：**
1. 去掉 jpy-section 整个区块
2. 新增 stance-jpy 元素显示日元Carry状态
3. 新增 key-signals 区块（合并关键变化+风险信号）
4. 图表区域改为 charts-core + chart-details（折叠）结构

**前端改动（`app.js`）：**
1. 新增 `renderKeySignals()` 函数，合并 key_takeaways + risk_flags + narrative_blocks
2. 修改 `renderCharts()` 函数，分离核心图表和背景图表
3. 修改 `renderTradingRadar()` 只显示 P0/P1

**前端改动（`styles.css`）：**
1. 新增 signal-card、signal-priority、signal-title 等样式
2. 新增 chart-details、chart-grid 等样式
3. 新增 radar-grid、radar-item 等样式

### 验证结果
- HTTP 服务（端口 8081）正常
- `tbill_combined_supply_30d` 图表已生成，27个数据点
- 前端页面已打开

### 12:16 只读检查发现的问题
- 当前前端/图表改动存在多处待修复问题：图表数据流仍在落库前生成，且部分图表直接/API式读取而非只读落库表；`jpy_usdjpy_funding_1y` 与 `us_jp_spread` 新增同名图表会遮蔽原 JPY carry 正确图表，口径错误；`tbill_combined_supply_30d` 实际只合并 T-bill，不是所有美债，且未按前20日+后10日窗口过滤；关键变化和风险信号存在重复展示；CSS 存在重复/冲突定义。

### 12:38 按用户指定问题完成修复
- **只修用户列出的问题**：未改未列出的功能方向
- 修复 `run.py` 流程：先生成基础 snapshot 并 `ingest_snapshot()` 落库，再从 SQLite 读取图表 series 生成 charts/dashboard，避免图表层读旧库或重复 API
- 修复 `charts.py`：新增 `db_metric_series()` / `db_derived_series()`，`fetch_chart_series()` 改为只读 SQLite，不再在图表阶段调用 FRED/NY Fed/Treasury API 或写 chart cache
- 修复 `dashboard.py`：删除错误重建的 `jpy_usdjpy_funding_1y` 和 `us_jp_spread`，保留 `jpy_carry.py` 生成的真实 USD/JPY+JPY funding 与 UST-JGB 利差图表
- 修复合并发行量图表：`tbill_combined_supply_30d` 改为 `treasury_combined_supply_30d`，从 `treasury_auctions_ts` 聚合所有 Treasury security types，窗口仅该图使用“前20日 + 后10日”
- 修复前端：`renderKeySignals()` 改为 risk 优先并去重，日元 Carry 状态从 analysis fallback 到 `dashboard.jpy_carry.risk.label`，核心图表从后端 `chart_groups` 读取而非前端硬编码
- 统一图表展示：前端渲染时用 `limitChartToRecentMonth()` 限制图表为最近约一月，并兼容 `YYYYMMDD` 日期格式
- 清理冗余 CSS：移除独立 JPY 区块样式和本次新增的重复 chart/radar/table 样式，仅保留必要 signal 与 chart-details 样式
- 同步更新 `prompts/usd_liquidity_prompt.md`：明确单一总判断、key/risk 不重复、图表来自本次落库后 DB、合并美债发行口径、JPY 图表口径
- 验证：Python AST 检查通过；Node `--check` 通过；完整 `scripts/run.py` 流水线成功；核心图表验证通过：`treasury_combined_supply_30d` 已生成，`jpy_usdjpy_funding_1y` 恢复 USDJPY+JPY_CALL，`us_jp_spread` 恢复 US_JP_2Y/US_JP_10Y

### 13:24 模型数值格式规则
- 用户要求模型输出数据保留两位小数即可。
- 已更新 `prompts/usd_liquidity_prompt.md` 字段规则：所有输出文本中的数值统一保留两位小数，禁止浮点长尾（如 `-2.9999999999999805bp`）。
- 已同步更新 `scripts/analysis.py` 的 `output_contract.number_format_rule`，保证 `model_input.json` 携带同一规则。

### 13:25 JPY Carry 分析主线修正
- 用户明确：关注的不是“美元流动性收紧对日元的冲击”，而是“借日元买美股/美元风险资产的流动性供给”。
- 已更新 `prompts/usd_liquidity_prompt.md` 的日元 Carry 叠加判断：主线改为 JPY融资成本/BOJ政策 -> 美日利差 -> USD/JPY趋势与波动 -> 仓位拥挤度 -> 借日元买美股/美元风险资产的杠杆资金供给 -> 美股/风险资产流动性边际变化。
- 已更新 `prompts/jpy_usd_carry_liquidity_prompt.md`：角色、分析原则、传导链、典型组合和一句话结论全部改为围绕“日元低息资金对美股/美元风险资产的流动性供给”。
- 已同步更新 `scripts/analysis.py` 的 `output_contract.jpy_carry_rule`，保证 `model_input.json` 携带该口径。

### 13:28 美日利差代理只有一天数据的来源排查
- 排查确认：`output/latest/jpy_carry_history.json` 和 `dashboard_data.json` 中 `us_jp_spread` 都只有1个点。
- 根因在 `scripts/jpy_carry.py`：虽然 FRED DGS2/DGS10 和 MOF JGB2/JGB10 原始时间序列都有充足历史，但 `us_jp_spread` 图表构建时只用 `dgs2_latest/dgs10_latest` 与 `jgb2_latest/jgb10_latest` 计算单个最新点，没有按日期对齐生成历史 spread series。

### 13:41 修复 JPY carry 数据合理性问题
- 修复 `utils.parse_date_guess()`：新增支持 `YYYYMMDD` 和 `YYYY/M/D`，解决 BOJ `20260603`、MOF `2026/5/29` 日期无法标准化的问题。
- 修复 `jpy_carry.point_before()`：日期解析失败或找不到阈值前数据时不再 fallback 到第一条历史数据，避免“逐日变化”被算成多年变化。
- 新增 `normalize_rows()` 和 `align_spread_previous_base()`，对 UST 与 JGB 序列按“同日或不晚于该日的最近 JGB 数据”对齐。
- `us_jp_spread` 从单点 bar 改为历史 line：`US_JP_2Y` 和 `US_JP_10Y` 各 260 个点。
- 验证修复后 JPY carry 卡片：USD/JPY 逐日 +0.01%，JPY_CALL 逐日 0.0bp，JGB10 逐日 -3.5bp，不再出现 +10.63% / -244.6bp 这类错误。
- 重新运行 `jpy_carry.py` 和主 `run.py`，刷新 `model_input.json` 与 `dashboard_data.json`；确认最新 `model_input` 已包含“借日元买美股”规则和“两位小数”规则。

### 13:46 美日利差工程落表化
- 用户要求“用工程逻辑落表，确保数值计算都是工程手段解决”。
- 新增 SQLite 表 `jpy_carry_series_ts`，字段包括 `series_id/series_name/category/as_of/value/unit/source/source_url/snapshot_file/ingested_at/notes`，用于持久化 JPY carry 原始序列和工程计算序列。
- `scripts/jpy_carry.py` 新增 `ensure_jpy_carry_table()`、`upsert_jpy_series()`、`load_jpy_series_from_db()`、`persist_jpy_carry_series()`。
- 美日利差 `US_JP_2Y`、`US_JP_10Y` 现在先通过工程函数 `align_spread_previous_base()` 计算，再写入 `jpy_carry_series_ts`，图表和模型输入再从 SQLite 读回，不再由前端或模型临时计算。
- 验证：当前 snapshot `jpy_carry_history_20260607_135010.json` 下，`US_JP_2Y` 和 `US_JP_10Y` 各落表 286 条，图表各输出 260 个点；`model_input` 与 `dashboard_data` 均读取连续的 `us_jp_spread` line 序列。

### 13:52 全量二次加工落表检查与补齐
- 用户要求检查其他位置的数据计算，确保所有二次加工数据都使用工程手段、落表，图只从表中取数。
- 已盘点并补齐：USD 流动性二次加工信号已由 `signals.py` 工程计算并随 snapshot 落入 `derived_signals_ts`，`charts.py.fetch_chart_series()` 只读 SQLite；不在图表阶段重复 API/模型计算。
- 新增通用工程表 `engineered_series_ts`，用于非 JPY 的工程聚合序列。
- `treasury_combined_supply_30d` 现在流程为：从 `treasury_auctions_ts` 工程聚合全部美债发行量 -> 写入 `engineered_series_ts.TREASURY_COMBINED_SUPPLY` -> 图表从该表读出。
- JPY carry 图表全部从 `jpy_carry_series_ts` 读取：`USDJPY`、`JPY_CALL`、`JGB2/JGB10/JGB30`、`CFTC_JPY_NET_OI`、`JPY_NEER/JPY_REER`、`USDJPY_VOL20`、`US_JP_2Y/US_JP_10Y` 均落表。
- 验证：最新 `jpy_carry_series_ts` 中 `US_JP_2Y/US_JP_10Y` 各 286 条，`USDJPY/JPY_CALL` 各 265 条，`USDJPY_VOL20` 245 条，`CFTC_JPY_NET_OI` 130 条；`engineered_series_ts.TREASURY_COMBINED_SUPPLY` 10 条。
- `dashboard_data.json` 与 `charts.json` 的核心图表均标注来源：`treasury_combined_supply_30d` 来自 `engineered_series_ts`，JPY 图表均来自 `jpy_carry_series_ts`。
