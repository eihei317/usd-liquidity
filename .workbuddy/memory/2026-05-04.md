# 2026-05-04

- 14:32 改造美元流动性简报流水线：新增 `prompts/usd_liquidity_intro.md`，强化 `prompts/usd_liquidity_prompt.md` 输出合同，更新 `scripts/usd_liquidity_brief.py` 生成 Markdown 简报、结构化 snapshot JSON 和独立 `usd_liquidity_model_input_*.json`；利率表固定为“利率/含义/边际变化”。已通过语法检查、离线快照验证和实时抓取验证。
- 14:45 继续优化美元流动性简报：移除人读简报中的模型 context/prompt 附录，新增数据频率与环比口径表，明确日频/周频/事件驱动/政策阶梯型数据不能混比；新增核心影响指标说明；生成最近一周和一月 SVG 折线图（短端利率、政策锚利差、TGA、RRP）。已实时运行并验证 `usd_liquidity_brief_20260504_144212.md`、snapshot、model_input、两张图表。
- 14:48 用户确认前端方案采用 A：稳定前端模板 + 模型只输出结构化 `analysis.json`；日常运行不让模型修改前端，只由前端确定性渲染分析结果和图表数据。
- 14:51 已定前端数据契约方向：`snapshot.json` 保留原始结构化抓数，`dashboard_data.json` 由脚本确定性生成展示骨架/指标卡/图表序列，`analysis.json` 由模型只输出结论、解释、风险和传导判断；前端只读这些 JSON 渲染。
- 14:58 落地“美元流动性监测”前端：新增 `frontend/usd-liquidity-monitor/index.html`、`styles.css`、`app.js`，米白 Claude 风格；脚本新增 `dashboard_data.json`、`analysis.json`、`charts.json` 和 `output/latest/` 输出，前端读取 latest JSON 稳定渲染。已生成 `usd_liquidity_*_20260504_145814` 产物并校验 JSON contract。
- 15:23 修正前端和分析文案：衍生信号统一改成"英文（中文翻译）"格式，`CP-政策上限 proxy` 改为 `CP Rate-Policy Upper Proxy（商业票据利率-政策上限代理利差）` 并解释其只是企业短融压力代理、不是FRA-OIS；关键变化文本改为更详细的分析段落。已生成 `usd_liquidity_*_20260504_152257` 并校验旧标签已移除。
- 15:45 前端图表改为 Chart.js 动态渲染：引入 Chart.js CDN，重写 app.js 图表渲染逻辑（`drawChartJS` 替代 `drawLineChart`），支持鼠标悬停显示当天利率的 tooltip 交互；移除旧的 SVG 绘图函数。图表时间轴自适应，tooltip 显示日期和各系列数值。
- 15:54 确认 IORB 的 canonical 来源就是 FRED `https://fred.stlouisfed.org/series/IORB`。脚本已调整为 FRED 指标的 `source_url` 暴露人读 series 页面，程序化 CSV/API 下载地址保留到 `extra.fetch_url`；当前本地抓取 FRED 仍超时，所以 IORB 值为 NA，并按既定规则降级使用 NY Fed EFFR 记录中的政策上限作为临时政策锚。
- 16:00 修复 Chart.js 图表滚动到图表区域时无限放大问题：原因是 canvas 直接挂在卡片内并写入 inline width/height，Chart.js responsive resize 与父容器高度计算互相触发；已改为固定高度 `.chart-canvas-wrap` 容器，canvas 100% 填充容器，并通过静态校验和前端预览确认。FRED 超时排查显示 `fred.stlouisfed.org` 的 series/csv/data 路径均 12 秒读超时，`api.stlouisfed.org` 能快速返回但无 `FRED_API_KEY` 会 400。
- 16:08 传导链判断改为完整链路展示：脚本新增 `build_transmission_chain()`，`analysis.json` 的 `transmission.chain` 固定输出六层（Fed负债端水位、政策锚/准备金边际、银行间无抵押融资、回购融资/抵押品链条、离岸美元、信用市场），每层有状态、证据、诊断、问题类型；前端 `renderTransmission()` 改为卡片式完整链路展示并高亮问题层，prompt/output_contract/技能文档同步要求完整传导链。
- 16:12 配置 FRED API：脚本新增 `get_fred_api_key()`，优先读环境变量，再读工作区私密文件 `.workbuddy/secrets/fred_api_key`；已用 FRED API 重跑 latest，IORB 取到 3.650%（2026-05-04），WRESBAL/DTWEXBGS/DCPN3M/HY OAS 均为 FRED API ok。同步修正 FRED API URL 输出脱敏，清理此前输出中的 `api_key` 查询参数，并把 WRESBAL 从 FRED 的百万美元口径归一到 USD bn。
- 16:20 按“能用 API 抓的数据都用 API”继续改造：FRED 指标函数重命名为 `fetch_fred_series`，FRED 指标串行抓取并加重试，避免并发触发 500；图表锚利差改用 FRED API 的 IORB 序列而不是 NY Fed target upper；修正 OFR STFM repo fails mnemonic 为 `NYPD-PD_AFtD_T-A` 并兼容 list row API 响应。最新校验所有核心指标均来自 FRED API、NY Fed Markets API、Treasury FiscalData/TreasuryDirect 或 OFR STFM，错误项为 0，明文 key 泄漏为 0。
- 16:31 生成项目交接文档 `PROJECT_HANDOFF_FOR_CODEX.md`，面向 Codex/新工程师接手，覆盖项目目标、目录结构、API/密钥配置、运行与预览命令、数据源、输出合同、传导链、前端设计、常见坑、验证清单和后续迭代建议；已校验文档不包含 FRED API Key。并沉淀用户级技能 `project-handoff-codex-md` 用于未来生成类似 Codex 交接文档。

