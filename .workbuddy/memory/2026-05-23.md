# 2026-05-23 工作记录

## 美元流动性监测数据更新与分析

- 运行 `scripts/usd_liquidity_brief.py` 成功抓取最新数据，输出：
  - 报告：output/usd_liquidity_brief_20260523_181204.md
  - 快照：output/usd_liquidity_snapshot_20260523_181204.json
  - 模型输入：output/usd_liquidity_model_input_20260523_181204.json
  - 前端数据：output/usd_liquidity_dashboard_data_20260523_181204.json
  - 分析结果（fallback）：output/usd_liquidity_analysis_20260523_181204.json
  - 图表：7d + 30d SVG 各一张
  - 清理旧产物：8 个

- 脚本判断：中性，分数 1.0

- 生成正式模型分析（覆盖 fallback analysis.json）：
  - 读取 model_input.json 中衍生信号取值表（16 个信号）
  - 按照 prompt schema 输出严格 JSON，写入 output/latest/analysis.json
  - 主要判断：中性（置信度中等）
  - 关键结论：
    1. SOFR-IORB = -14bp，短期回购融资不紧
    2. RRP 余额 0.96bn 接近耗尽，后续冲击将直接打到准备金（P1 风险）
    3. 10Y 实际收益率 2.18%（+5bp），成长股/黄金估值承压（P1 风险）
    4. VIX 16.76（-0.68），风险偏好尚可

- 数据新鲜度问题：
  - 多数日频指标 as_of 2026-05-21（滞后 2 个交易日）
  - IORB 显示 as_of 2026-05-26（未来日期，疑似数据源异常），分析时以 SOFR-IORB 衍生信号为准
  - NFCI / DTWEXBGS 周频数据滞后约 8 天

- 同步前端：将 output/latest/dashboard_data.json 和 analysis.json 复制到 frontend/usd-liquidity-monitor/，预览前端界面确认渲染正确。

## 补充日元 Carry Trade 风险雷达

- 在 `scripts/usd_liquidity_brief.py` 中新增 `build_jpy_carry_overlay()`，dashboard_data 现在包含 `jpy_carry`。
- 日元 Carry 雷达覆盖：
  - USD/JPY 点位、5日/20日变化、20日实现波动率（FRED `DEXJPUS`）
  - 美日10Y利差（美国 DGS10 + 日本10Y代理 `IRLTLT01JPM156N`）
  - 美日短端利差代理（美国 DGS2 + 日本3M代理 `IR3TIB01JPM156N`）
  - CFTC日元投机净头寸（CFTC 097741）
  - VIX + HY OAS 全球风险偏好确认
- 当前日元 Carry 判断：风险中性，评分 0.0/6。关键证据：USD/JPY 158.69，5日 +1.3%，20日 -0.4%，20日实现波动率约 9.5%，VIX 16.76；CFTC 日元净空头 -93,905 作为背景拥挤风险。
- 修复前端读取逻辑：`app.js` 现在优先动态读取 `./dashboard_data.json` / `./analysis.json`，file:// 预览失败时才回退 `data.js`；同时更新 `data.js` 静态数据，避免页面继续显示旧数据。
- 修复 `data_as_of`：过滤 IORB 异常未来日期，避免 dashboard meta 显示 2026-05-26。
- 已通过 `py_compile`、脚本重跑、前端 JS 语法检查，并预览确认日元 Carry Trade 风险雷达已渲染。

## 接回 JPY Carry 旧链路并重构为固定流水线

- 恢复独立 JPY Carry 数据脚本：新增 `scripts/jpy_carry_history.py`。
- 新链路固定为：
  1. `jpy_carry_history.py` 抓取 BOJ / MOF / CFTC / FRED 数据；
  2. 输出 `output/latest/jpy_carry_history.json`；
  3. 读取 `prompts/jpy_usd_carry_liquidity_prompt.md` 生成 `output/latest/jpy_carry_model_input.json`；
  4. 模型分析可覆盖 `output/latest/jpy_carry_analysis.json`；
  5. `usd_liquidity_brief.py` 自动刷新并读取 `jpy_carry_history.json`，写入 `dashboard_data.json.jpy_carry`，并将 JPY charts 追加到 dashboard charts；
  6. 前端固定读取 JSON 渲染，不让模型输出 HTML。
- JPY Carry 恢复的数据源：
  - BOJ `FM08/FXERD04`：USD/JPY
  - BOJ `FM01/STRDCLUCON`：JPY隔夜融资成本
  - BOJ `FM09/FX180110001, FX180110002`：NEER / REER
  - MOF `jgbcme_all.csv`：JGB 2Y / 10Y / 30Y
  - CFTC Public Reporting `097741`：日元非商业净仓位/OI
  - FRED：DGS2 / DGS10 / VIXCLS / HY OAS
- 关键修复：CFTC 日期字段形如 `2026-05-19T00:00:00.000`，解析前需去掉 `T...`，否则仓位会显示 NA。
- 最新 JPY Carry 判断：中性偏高，评分 3.0/6；证据为 CFTC JPY净仓位/OI -23.7%、JGB10Y 2.520%（约60日 +38.8bp）、10Y UST-JGB 利差约 205bp；USD/JPY 159.07、20日 -0.67%、20日实现波动率 8.84%，尚未触发 unwind。
- 验证：`py_compile` 通过；`jpy_carry_history.py --json` 通过；`usd_liquidity_brief.py --json` 通过；前端 JSON 和 `data.js` 已同步；预览成功。
