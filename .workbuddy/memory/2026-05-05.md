# 2026-05-05

- 13:24 更新美元流动性监测数据：运行 `scripts/usd_liquidity_brief.py "今天美元流动性监测" --json`，生成 `usd_liquidity_*_20260505_132318` 系列产物并刷新 `output/latest/`。最新总判断为“中性偏松”（规则分数 -0.8），数据日期 2026-05-05；所有核心指标 API 状态 ok，错误项 0，FRED API Key 明文泄漏检查为 0；本次 TGA 降至 857.3bn，RRP 约 0.6bn，IORB 3.650%，EFFR/SOFR 分别 3.640%。
- 13:35 优化美元流动性前端和信号：`DerivedSignal` 增加 `previous`/`change`，关键变化中的 EFFR-IORB、SOFR-IORB、SOFR-EFFR、BGCR-TGCR、CP Proxy 等利差展示当前值、上一期和边际变化；图表区移到关键变化后，新增关键利差图（含 BGCR-TGCR）和信用传导代理利差图（CP Proxy）；核心指标由卡片改为表格。已重新生成 `usd_liquidity_*_20260505_133434`，JS/Python 校验通过，密钥泄漏检查为 0。
- 14:52 将美债收益率/曲线和金融条件代理纳入框架：新增 FRED API 指标 DGS3MO、DGS2、DGS10、DGS30、T10Y2Y、T10Y3M、NFCI；新增曲线利差衍生信号 `UST_10Y2Y`、`UST_10Y3M` 和 `NFCI_LEVEL`；前端图表新增美国国债收益率、美债长短端利差、公开金融条件代理图；核心指标表新增“国债收益率与曲线”和“NFCI”。调研确认高盛 FCI 为专有指标，未找到免费官方公开 API，已加入 unavailable_sources，并使用 NFCI 作为明确标注的公开代理。已生成 `usd_liquidity_*_20260505_145158`，校验密钥泄漏为 0。
- 15:08 重构日常交易版前端与输出清理：脚本新增 `trading_dashboard` 数据结构，将指标分为 core/confirm/background，首页核心为 8 个 P0/P1 指标；前端新增“日常交易雷达”卡片区，只显示核心交易图，二级确认与背景指标折叠，全量指标表默认折叠；脚本新增 `cleanup_old_outputs()`，每次成功更新后清理旧 timestamp 产物，只保留当前运行与 `output/latest/`。本次运行生成 `usd_liquidity_*_20260505_150748`，清理旧产物 144 个，output 中 timestamp 产物剩 8 个，密钥泄漏检查为 0。
- 15:15 修复“日常交易雷达为空”：确认 `output/latest/dashboard_data.json` 已有 `trading_dashboard.core=8`，问题主要是浏览器缓存旧 `app.js/styles.css` 导致新增 section 未被新 JS 渲染；已给 `index.html` 的 CSS/JS 引用加 `?v=20260505-radar` cache-busting，并给 radar 容器加默认占位文本。前端已用带版本参数 URL 重新预览。
- 15:22 精简并分类图表：`trading_dashboard` 新增 `chart_groups`，分为“核心资金压力”（关键利差7d、Fed负债端30d）、“资产定价与传导确认”（美债收益率30d、CP代理利差30d）和默认折叠的“背景复盘”（短端资金30d、关键利差30d、美债曲线30d、NFCI）。前端 `renderCharts()` 改为按分组渲染，背景组用 details 折叠；静态资源版本更新为 `?v=20260505-chartgroups`。已生成 `usd_liquidity_*_20260505_152216`，清理旧产物 8 个，密钥泄漏检查为 0。
- 15:41 按用户反馈优化图表：Fed负债端图改为双坐标轴，TGA 用左轴、RRP 用右轴，避免 RRP 因量级太小不可见；美国国债收益率图精简为 1Y 和 10Y，新增 FRED 指标 `DGS1`；美债长短端利差图从背景复盘上移到“资产定价与传导确认”默认展示。前端 Chart.js 新增 dual-axis 支持，静态资源版本更新为 `?v=20260505-dualaxis`；已生成 `usd_liquidity_*_20260505_154044` 并校验通过。
