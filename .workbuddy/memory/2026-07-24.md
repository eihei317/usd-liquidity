# 2026-07-24

- 完成美元流动性项目与 HY3 适配审计，报告：`output/usd_liquidity_prompt_audit_20260724.md`。核心发现：当前 `model_input.json` 620,669 bytes，紧凑 JSON 260,951 字符，JPY 全历史/图表占主要冗余；裁剪、去重后可降至约 70,618 字符（约 -73%）。
- Prompt/分析逻辑的优先问题：重复指标未做权威源归一；“禁止模型计算”与要求 bp/5Y-3Y/7Y-5Y 斜率冲突；`REAL_10Y` 实际装 DGS10 名义收益率且与 DFII10 真正实际收益率混淆；未来拍卖毛规模被越级解释为准备金净冲击；最新分析 384bn 与逐项 479bn 自相矛盾；CFTC JPY driver 只要 gross short 上升就判 `short_building`，在本期多头增加更多、short_share 下降时会误判 carry 流量。
- HY3 官方适配结论：`hy3` 为 256k 上下文、最大输入 192k；正式版文档未明确承诺 `response_format/json_schema/strict`。推荐使用精简 canonical facts + function tool JSON Schema + 本地 validator/修复重试 + candidate 原子发布。建议把 Prompt、metric contract、output schema 改为单一真源生成，替代人工双向同步。
- 已同步修订用户级 skill `usd-liquidity-brief-pipeline`：更新 1Y/3Y/5Y/7Y 当前收益率口径、移除旧 1Y/3Y/10Y 与 DGS30 说明、修正 HTTP 预览无需手动同步 data.js、改用 managed Python，并加入 HY3 输入裁剪、重复指标归一、拍卖 gross/net、CFTC driver 与结构化输出校验等坑点。

---

### 流水线运行（更新数据并分析）

- 运行 `scripts/run.py "今天的美元流动性简报" --json`：43 metrics + 24 derived_signals 入库，7 upcoming auction details
- 运行 `scripts/jpy_carry.py "更新日元carry数据" --json`：risk_label=中性偏高 risk_score=2.0
- 清理 337 个旧时间戳输出文件

### 模型分析（总判断：中性偏紧，confidence=中等）

**核心发现：**
1. **国债收益率腹部全线走高**：7/23 Treasury同日 1Y 4.15%(+4bp) / 3Y 4.40%(+6bp) / 5Y 4.46%(+5bp) / 7Y 4.58%(+5bp)，3Y领涨为最大边际变化。腹部斜率 5Y-3Y=6bp(上期7bp，略平坦化)，7Y-5Y=12bp持平
2. **RRP缓冲垫仅剩$0.9bn**：RRP_BUFFER=$0.904bn(7/23)，缓冲垫几近耗尽。未来一周$384bn美债供给将在零缓冲下直接考验准备金
3. **TGA单日大降$39.6bn**：TGA $835.4bn(7/22)，短期释放准备金但未来补库风险积累
4. **融资市场平静**：SOFR 3.62% vs IORB 3.65%(-3bp偏松)，SOFR日交易量$3,026bn健康
5. **信用/风险偏好平稳**：IG OAS 0.78%持平，HY OAS 2.68%(-1bp)，VIX 16.64(-0.41)回落
6. **JPY Carry**：CFTC driver=short_building(空头+2,603口)，空头占比67.3%(88分位)，USD/JPY 162.94

**风险排序：**
- P0 market: 国债收益率全腹段上行(3Y+6bp领涨)
- P0 market: RRP缓冲垫耗尽+未来一周密集供给
- P1 market: JPY carry拥挤(空头88分位)叠加短端再定价
- P1 data: DCPN3M数据严重滞后(7/21 vs 上期6/26，间隔26天)
- P2 data: CFTC/NFCI/DTWEXBGS/repo fails 周频滞后

### 输出与提交
- `output/latest/analysis.json` 模型结构化分析已覆盖
- HTTP server 端口 5678 启动用作预览
- Git commit c3b9e99 + push to origin/main 完成

---

### 分析逻辑与 HY3 适配全量修正（最终状态）

- 已重构 `model_input.json` 为 canonical facts：43 原始 metrics 归一为 34 个唯一指标，重复 ID 为 0；删除 fallback stance/score/highlights、JPY history/charts 与 chart_paths。紧凑输入从审计时 260,951 字符降至 74,202 字符，最终文件约 128KB。
- 新增 1Y/3Y/5Y/7Y bp 变化、腹部组合动量、5Y-3Y/7Y-5Y 斜率；10Y 明确拆为 DGS10 名义与 DFII10 实际信号。收益率组合计分设上限，避免四期限重复加总。
- 未来拍卖预聚合为 auction date / issue date / security type / grand total；当前 gross announced total=479bn，maturities/net cash impact/reserve impact 保持 null，Prompt 禁止把毛发行直接写成准备金抽水。
- CFTC JPY driver 修正为多状态归因；当前数据识别为 `two_sided_building_long_dominant`（空头+2,603、多头+3,718、short_share下降），`carry_usd_support=false`，纠正旧分析的 `short_building` 误判。
- Prompt 改为四轴 `funding_price / liquidity_buffer / treasury_pricing / cross_market_transmission`；P0 必须是已实现市场压力且至少两个独立新鲜事实确认，低 RRP / gross auction 单独最高为 P1；takeaway 只允许 observed，risk 使用 inference/scenario 并以 dedupe_key 去重。
- 新增 `scripts/analysis_validator.py`：完整 HY3 tool schema、字段/枚举/事实 ID/数值/P0/去重/预测校验和原子发布；新增 `scripts/hy3_analysis.py`，支持 TokenHub `hy3`、thinking high、tool call、校验后发布。dry-run 请求约112KB。
- 最终 `analysis.json` 已通过 validator 并原子发布，总判断“中性偏紧”，无 P0；前端实测通过，并将交易雷达的 CORE/CONFIRM/BACKGROUND 监控层级与模型 P0/P1/P2 风险优先级彻底分离。
- 验证通过：全模块 py_compile、Node `--check`、`git diff --check`、validator 正反测试、完整 snapshot 回放、HY3 dry-run、本地浏览器渲染。
- 收尾完成：将本轮全部修复与产物提交为 Git commit `7e9114c`（`feat: harden usd liquidity analysis contract for hy3`）并已同步到 `origin/main`；当前工作区与远端 `main` 对齐，无剩余未提交改动。
- 用户新增输出偏好：凡在分析里提到具体指标，统一写成“数量（更新时间，环比变化）”。已同步修改 `prompts/usd_liquidity_prompt.md`、`scripts/analysis.py`、`scripts/analysis_validator.py`，用 `output/latest/snapshot.json` 回放生成新 `model_input.json`，并重新校验/原子发布 `output/latest/analysis.json`；当前最终分析已符合该格式规则。

### 文案铁律进一步收紧（所有叙述性数字彻底统一）

- 用户要求“更严格一点：把所有 narrative block 里任何带数字的地方都彻底统一”。已把规则从“具体指标数值”升级为覆盖全部叙述性 prose（含 stance、axis summary）。
- 直接修正 `output/latest/analysis.json` 残留的非指标数字：日期简写 `7/22-23` → `2026-07-22 至 2026-07-23`；裸写 `stale_days 7 / 16` → 指标后 `已滞后 7 日 / 已滞后 16 日`；`previous_as_of 为 2026-06-26` → `上期为 2026-06-26`；`P0 交叉确认` → `P0 级交叉确认`。
- 强化 `scripts/analysis_validator.py`：新增 `_iter_narrative_text_fields` 遍历 stance/axis/takeaway/risk/narrative_blocks 全部 prose 字段，对带单位数值强制“数量（更新时间，环比变化）”并禁止 M/D 简写日期；豁免 related_indicators、priority/severity/type 枚举、metric ID、`已滞后 N 日`、`P0 级` 等。
- 同步更新 `prompts/usd_liquidity_prompt.md` 与 `scripts/analysis.py` 注入规则（indicator_mention_rule / number_format_rule / freshness_evidence_rule）为同一严格口径。
- 当前 `output/latest/analysis.json` 通过 validator（`status: valid`，0 errors）。本轮修改尚未 git 提交（用户选择“更严格统一”而非“提交并 push”）。
