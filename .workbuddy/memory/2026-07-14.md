# 2026-07-14

## 开源前密钥安全审计

用户要求为开源清理项目内所有 apikey。审计结论：**代码层无需清理，项目已可安全开源**。
- scripts/*.py 无硬编码密钥，统一从 env `FRED_API_KEY` 或 `.workbuddy/secrets/fred_api_key` 读取，URL 有 `redact_url_secret` 脱敏。
- output/ 317 个 JSON 的 fetch_url 全部为 `api_key=REDACTED`，无真实值残留。
- `.gitignore` 已忽略 `.workbuddy/secrets/` 与 `.workbuddy/cache/`。
- git 全历史 blob 扫描：FRED key（32字符）与 GitHub token 均未进入任何提交（安全）。
- 唯一敏感残留在本地 `.git/config` 的 remote URL：`https://x-access-token:ghp_...@github.com/eihei317/usd-liquidity.git`。该 token 只存本地 .git/config，不随 clone/开源同步，非开源泄漏点（除非整包 .git 外发）。仓库地址 = https://github.com/eihei317/usd-liquidity。
- 用户决定：保留本地 fred_api_key 文件、暂不改 token、不加配置文档。故本次未修改任何文件。

## 完整流水线运行 + GitHub push 测试（2026-07-14 15:xx）

- 触发语：「更新数据并分析 测试下push到github行不行」。完整跑通三段式流水线。
- `scripts/run.py` 抓取 41 指标 + 22 衍生信号，落库成功；规则 stance=中性(0.8)。
- 模型（AI agent）读取 `output/latest/model_input.json` 强制取值表 + `prompts/usd_liquidity_prompt.md`，生成 `analysis.json` 覆盖 `pending_model_analysis` 占位符：4 条 key_takeaways + 3 条 risk_flags；narrative.treasury_yields=偏紧、narrative.jpy_carry=中性。
- 关键判断：1Y/3Y/10Y 名义收益率全线 +6~8bp（一阶高权重偏紧）、TBILL_AUCTION_STRESS 跳升至 50（规模 195bn + BTC 降至 2.79x）、RRP_BUFFER 仅 0.8bn 接近耗尽；vs 回购（SOFR-IORB -10bp）/TGA 日降 6.4bn/信用/NFCI 宽松对冲 → 总判断「中性」。
- JPY carry：CFTC driver=`mixed`（空头 -30,939 口、多头 +375 口，非净加仓），short_share 67.8% 近1年 90% 分位高位；USD/JPY 161.70 高位小幅回落。不能下「carry 加杠杆」结论。
- **前端 server 必须从项目根目录起**（`python -m http.server 8000`），否则前端 `fetch('/output/latest/analysis.json')` 会因绝对路径 404；之前误从 `frontend/usd-liquidity-monitor/` 子目录起会导致该路径错误。
- **GitHub push 测试通过**：`git push origin main` 成功，`git ls-remote origin main` 确认 `origin/main = a8d7e56...`（与本地 HEAD 一致）。`failed to store: 100001` 仅是 macOS keychain 无法缓存含 token 的 remote URL 的警告，不影响认证/推送。`.gitignore` 忽略带时间戳的 `output/usd_liquidity_*` 与 `*.db`，仅追踪 `output/latest/` 与 `frontend/usd-liquidity-monitor/data.js`；故 SQLite 落表数据未入库提交（已知约束）。
- **前端预览 server = 定时关、不常驻**：用户明确要求不常驻，改为启动后定时自动关闭。实现：`run_in_background` 起 server（task），再起一个 `sleep 1800 && pkill -f "http.server 8000"` 的清理 task（约30分钟自动释放端口）。注意 macOS 该 shell **无 `timeout` 命令**（`command not found: timeout`），不能用 `timeout 1800 python3 ...` 包裹，必须用 `sleep N && pkill` 方案。需要临时预览时执行 `cd /Users/eiheisun/WorkBuddy/2026-05-04-task-1 && python3 -m http.server 8000 --bind 127.0.0.1`（项目根目录）。2026-07-01 的 `loadJson` 修复仅去掉「手动同步 data.js」，前端仍用 `fetch('/output/latest/analysis.json')` 绝对路径读取，**file:// 直接打开会因 CORS 失败，server 仍必须**。

## 美债收益率监控口径变更：30Y → 1Y/3Y/5Y/7Y 腹部组合（2026-07-14 20:xx）

- 用户指令：「以后不要监控30y了 监控1357」。经确认采用方案 C：**1Y/3Y/5Y/7Y 作为主分析框架，10Y 仅作曲线/折现率背景（不再是一阶高权重维度）；同时去掉 30Y（DGS30）**。
- 双向同步（铁律 2026-07-06）：prompt 与 scripts 均改。
  - 抓取：`scripts/fetchers.py` 的 `fetch_treasury_par_yield_curve` 与 `fetch_all_metrics` 增加 DGS5/DGS7、移除 DGS30。
  - 定义：`scripts/utils.py` 的 METRIC_LABELS/RATE_MEANINGS/DATA_FREQUENCY_RULES 增 DGS5/DGS7、删 DGS30；CORE_INDICATOR_IMPACTS 改述为「1Y/3Y/5Y/7Y腹部组合」。
  - 衍生信号：`scripts/signals.py` 仿 UST_1Y/3Y_YIELD 新增 UST_5Y_YIELD、UST_7Y_YIELD。
  - 展示/图表：`scripts/dashboard.py` 的 treasury_curve 分组、signal_base_metrics、core（UST_5Y/7Y 升 P1、REAL_10Y 移出）、confirm（REAL_10Y_MOMENTUM 移出）、background（加入 DGS5/DGS7/DGS10/NOMINAL_10Y/REAL_10Y/REAL_10Y_MOMENTUM），以及 `treasury_yields_30d` 图表系列改为 1Y/3Y/5Y/7Y(+10Y背景)；`scripts/charts.py` metric_ids 用 DGS5/DGS7 替 DGS30；传导图节点 bond_pricing 同步。
  - 规则注入：`scripts/analysis.py` 的 `_SIGNAL_BASE_METRICS`、`treasury_yields_rule`、两处 fallback priority、`narrative_blocks.treasury_yields` 全部改为 1Y/3Y/5Y/7Y 主框架 + 10Y 背景。
  - prompt：`prompts/usd_liquidity_prompt.md`（衍生信号取值表、收益率章节、JSON 示例、输出契约）与 `prompts/usd_liquidity_intro.md` 第7条同步。
- 验证：`run.py` 抓到 43 指标，snapshot 中 DGS5/DGS7 FOUND、DGS30 MISSING；`dashboard_data.json` 与 `treasury_yields_30d` 系列正确；模型生成 `analysis.json`（stance 中性偏紧，腹部 1Y/3Y/5Y/7Y 全 +6~8bp 为一阶信号）。
- 注意：此次口径变更推翻了早期「10Y=DGS10 是主指标/折现率锚」约定，改为 10Y 仅背景；若日后要再调整腹部组合，须同样两端同步。
