# 2026-06-18

- 收益率主分析从 10Y TIPS real yield 切换为 10Y Treasury Yield（DGS10）；新增 `UST_3Y_YIELD` 衍生信号，`REAL_10Y` / `REAL_10Y_MOMENTUM` 暂作兼容字段但底层改为 DGS10。
- 更新 `prompts/usd_liquidity_prompt.md`、`prompts/usd_liquidity_intro.md` 和 `scripts/analysis.py` 输出约束，要求模型输出 `narrative_blocks.treasury_yields`，并明确 1Y=近端政策路径、3Y=中段再定价、10Y=长期折现率锚。
- 运行最新流水线并同步 `frontend/usd-liquidity-monitor/data.js` 的 `DASHBOARD_DATA` 与 `ANALYSIS_DATA`，避免 file:// 预览回退到旧数据。
- 根据用户反馈，取消前端独立“短债与长债：1Y / 3Y / 10Y”模块；1Y/3Y/10Y 收益率分析只在模型分析/关键变化文字中体现，不单独占一个页面区块。
- 清理独立收益率模块残留代码：移除 `dashboard_data.treasury_yield_analysis`、`build_treasury_yield_analysis()`、前端渲染函数/样式/HTML 区块；`run.py` 改为每次新数据生成后重置 `analysis.json` 为 `pending_model_analysis`，防止旧分析复用，必须由模型覆盖后再同步 `data.js`。
