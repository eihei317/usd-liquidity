# 2026-07-20 工作日志

## 更新数据并分析（完整流水线）

- 触发词「更新数据并分析」，按 skill `usd-liquidity-brief-pipeline` 全流程执行：
  1. `scripts/jpy_carry.py` 抓 JPY carry（risk 中性偏高 score 2.0）。
  2. `scripts/run.py` 抓最新美元流动性数据：43 metrics + 24 derived_signals 入库，snapshot=20260720_104224，规则 fallback stance=中性(0.60)，清理旧文件 8 个。
  3. 模型读取 `output/latest/model_input.json` + prompt，生成结构化 `analysis.json` 覆盖占位符。
  4. 本地预览：`python3 -m http.server 8891 --bind 127.0.0.1`（注意：不加 --bind 时默认可能被本机代理拦截返回 502，且 IPv6-only 绑定导致 --noproxy 探活失败；用 `--bind 127.0.0.1` + `curl --noproxy '*' http://127.0.0.1:8891/...` 探活最稳）。
  5. git commit + push 成功（origin/main=d506aae）。push 时 "failed to store: 100001" 为凭据缓存告警、"Everything up-to-date" 为二次调用，实际已推送成功。

## 本次分析结论（2026-07-20，数据 as_of 2026-07-17）

- stance=中性（confidence 中等，规则分 0.60）。
- 融资端宽松：SOFR 3.62% 低于 IORB 3.65%，SOFR-IORB -3bp；SOFR_VOLUME 3,038bn；EFFR/OBFR 低于 IORB，准备金充裕。
- 结构性偏紧两项（均入 risk_flags P1/market）：TGA 日增 +26.5bn 补库加速抽水；RRP 缓冲垫仅 0.1bn 几近耗尽。
- 收益率腹部组合温和：1Y 4.01%(+2bp)/3Y 4.21%(+1bp)/5Y 4.28%(0)/7Y 4.40%(-1bp)，均 <3bp 阈值；10Y 背景 4.55%(-2bp)。
- 信用偏松：IG 0.78%(-1bp)、HY 2.71%(持平)、VIX 16.73(+1.06 低位)、NFCI -0.54。
- JPY carry：driver=short_building（空头周增 +2,603 口）当下支撑美元，但空头占比 67.3% 近1年88分位拥挤，美日10Y利差 188bp 偏窄 → 风险中性偏高。
- T-bill 吸收平稳（STRESS 20↓、SIZE 95bn、BTC 2.84x）；未来一周发行密集（07-20 13wk $92bn+26wk $79bn、07-21 6wk $95bn、07-23 10Y $21bn）。
