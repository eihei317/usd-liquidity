# 2026-07-31

## 美元流动性流水线（"更新数据并分析" 触发）

- 运行 `scripts/run.py "更新数据并分析" --json`，落库快照 `usd_liquidity_snapshot_20260731_151526.json`：吸入 52 个原始指标、45 个衍生、4 条拍卖明细；cleanup_removed 7。脚本初始 stance = 中性偏紧，score 2.5。
- 模型自主读取 `model_input.json` + `prompts/usd_liquidity_prompt.md`，生成结构化 `analysis.json`（四轴 + 三期限：immediate/tactical/structural；5 takeaways + 4 risk_flags）。
- 校验 `analysis_validator.py` 三轮迭代后 `status: "valid"`、`errors: []`。关键坑：① `generated_at_bjt` 必须用 model_input 顶层 15:15:46（非 JPY meta 的 15:15:26）；② 收益率不可分组写 `4.04%/4.30%/...`，必须逐期限带 `（date，change）`；③ FRBSF 变化用原生 `%`（如 `−0.009%`），不能用 bp 改写；④ 历史分位写 `历史分位 97.0`（不带 %）；⑤ 全文件必须用 ASCII 减号 `-`，Unicode `−` 会被正则判为非负数导致追溯失败。
- 提交 `b4c41ae`（18 files, +8966/−5700），并 push `origin/main` 成功（Everything up-to-date，远端已含本提交）。
- 本地预览 `http://127.0.0.1:8765/frontend/usd-liquidity-monitor/index.html` 验证 200。
- 综合判断：中性偏紧（中置信）；无 P0（无双证据融资压力）；P1 = RRP 缓冲近耗尽 + 准备金回落的结构脆弱性；P2 = T-bill 毛供给情景、JPY carry 拥挤、部分数据滞后（REPO_FAILS_UST 滞后16日、DTWEXBGS/NFCI 滞后7日、WRESBAL/SOMA 周频）。
