# 2026-06-04 工作记录

## 完整流水线执行

- 运行 `scripts/run.py "更新数据并分析" --json` ✅
  - 33 metrics + 18 derived signals 入库
  - JPY carry 7 个指标卡已抓取（USDJPY 159.69、JPYCALL 0.727%、US_JP_10Y 180bp、CFTC_JPY -26.8% 等）
  - `upcoming_auctions` 状态 ok，今日 4-week/8-week T-bill 各 75bn

- 模型生成 `analysis.json` ✅
  - stance: 中性偏松，confidence: 中
  - 新增 `jpy_carry` 字段（prompt 输出合约已加此字段）
  - `narrative_blocks.jpy_carry` 包含 Carry 分析段落

- 前端修复 ✅
  - **根本原因**：HTTP server 一直有路径/端口问题，前端 `fetch()` 拿不到数据
  - **解决方案**：改内嵌数据模式，把 `dashboard_data.json` 和 `analysis.json` 直接注入 `index.html` 为 `window.DASHBOARD_DATA` / `window.ANALYSIS_DATA`
  - `app.js` 第 486/493 行已正确读取内嵌变量
  - 不再依赖 HTTP server，直接 `preview_url(file://...)` 打开即可

## JPY Carry 工作流确认

- **数据层** ✅ `run.py` 调用 `refresh_jpy_carry_history()` → `dashboard_data.json` 有 `jpy_carry`（7 cards）
- **分析层** ✅ `prompts/usd_liquidity_prompt.md` 输出合约已加 `jpy_carry` 字段 → `analysis.json` 含 `jpy_carry`
- **前端层** ✅ `app.js` `renderJpyCarry()` 函数存在，现在能读到 `dashboard.jpy_carry` 数据并渲染

## 前端字段名/格式不匹配 Bug 修复

- **Bug 1**：`app.js:33` 显示"暂无模型分析" — `analysis.json` 用 `stance.summary`，前端期望 `stance.one_liner`
  - 修复：`stance.one_liner || stance.summary || "暂无模型分析"`，兼容两个字段名
- **Bug 2**：`app.js:52` takeaways/risk-flags 显示"提示"且内容为空 — `analysis.json` 是字符串数组，前端期望对象数组
  - 修复：`renderStack` 中 `typeof item === "string"` 时直接渲染字符串内容，兼容两种格式

## 数据风险

- WRESBAL stale_days=8（截至 2026-05-27），下次 H.4.1 发布日（通常周三）需关注
- T-bill 拍卖数据依赖 Treasury FiscalData API，已修复 SSL 问题（用 `requests` 优先）

## 前端回退与稳定化

- 定位到前端被改坏的根因不是数据缺失，而是两类预览/渲染问题叠加：
  - `index.html` 被错误注入大段 `window.DASHBOARD_DATA` / `window.ANALYSIS_DATA` 内嵌 JSON，且注入位置在 `app.js` 之后，`app.js` 已执行完 `init()` 后才赋值，无法稳定生效，还会膨胀模板。
  - `run.py` 每次只把 placeholder analysis 写进 `frontend/usd-liquidity-monitor/data.js`，模型覆盖 `output/latest/analysis.json` 后若不再同步 `data.js`，`file://` fallback 会读到 pending analysis。
- 已回退错误做法：移除 `index.html` 内嵌 JSON，恢复为静态模板 + `data.js`/`app.js`；同步最新 `output/latest/dashboard_data.json` + `analysis.json` 到 `frontend/usd-liquidity-monitor/data.js`；更新 query string 破除缓存。
- 保留必要 schema 兼容修复：`renderStance` 兼容 `stance.summary`，`renderStack` 兼容字符串数组和对象数组，避免再次出现“暂无模型分析”或空卡片。
