# 2026-05-21 工作记录

## 美元流动性监控项目 - 前端简化

按照用户要求简化了前端界面，删除冗余段落并同步修改提示和脚本：

### 用户需求
1. 模型利率表、模型数据删除（已有数据质量表）
2. 后续观察删除
3. 日元carry trade 移到最上面（与美元流动性hero在一起）
4. 传导链删除

### 完成的修改

#### 1. 前端文件（上一轮会话完成）
- `frontend/usd-liquidity-monitor/index.html`：删除4个section（freshness-title、rate-table-title、watchlist-title、transmission-title），移动jpy-section到hero之后
- `frontend/usd-liquidity-monitor/app.js`：删除5个函数（transmissionStatusClass、renderTransmission、renderAnalysisFreshness、renderAnalysisRateTable、renderWatchlist）
- `frontend/usd-liquidity-monitor/styles.css`：重写，删除~400行未使用样式

#### 2. 提示文件 `prompts/usd_liquidity_prompt.md`
- 判断模板：删除第5项"传导判断"和第6项"后续观察"
- 字段规则：删除 `transmission.chain` 和 `rate_table` 相关规则

#### 3. Python脚本 `scripts/usd_liquidity_brief.py`
- 删除函数：
  - `build_analysis_freshness()` (原1965-1999行)
  - `build_analysis_rate_table()` (原2002-2029行)
  - `build_fallback_watchlist()` (原2069-2095行)
- 更新 `output_contract`：
  - `required_top_level_fields` 改为 `["meta", "stance", "key_takeaways", "risk_flags", "narrative_blocks"]`
  - 删除 `rate_table_columns`、`rate_name_format`、`rate_table_language_rule`、`transmission_chain_rule`
  - 简化 `json_schema_summary`
- 更新 `build_fallback_analysis()`：删除 `freshness`、`rate_table`、`transmission`、`watchlist` 字段

### 验证
- Python语法检查通过
- 脚本成功运行：`/usr/bin/python3 scripts/usd_liquidity_brief.py "简报" --json`
- 生成 `analysis.json` 只包含 `meta`, `stance`, `key_takeaways`, `risk_flags`, `narrative_blocks`
- 前端成功打开并渲染

### 技术笔记
- 使用Python脚本批量修改比Edit工具更可靠（避免字符串匹配失败）
- 修改后必须用 `py_compile` 检查语法
- 确保JSON schema、提示字段规则、脚本输出、前端渲染四者一致
