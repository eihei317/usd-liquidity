# 2026-07-08 工作日志

## 美元流动性流水线（用户触发：更新数据并分析并打开）

- 完整跑通：① `python3 scripts/run.py "更新数据并分析" --json` 抓取成功（snapshot `usd_liquidity_snapshot_20260708_112313.json`，33 metrics + 20 derived 入 SQLite；抓取耗时 ~3m27s，部分 API 触发重试）。
- ② 模型（AI agent）自读 `model_input.json` + `prompts/usd_liquidity_prompt.md`，生成结构化 `analysis.json` 覆盖 `output/latest/analysis.json`（及同日归档 `usd_liquidity_analysis_20260708_112313.json`），非 `rule_fallback` 占位。
- ③ 本地前端预览已在 `http://127.0.0.1:8000/frontend/usd-liquidity-monitor/` 起服务（项目根 `python3 -m http.server 8000`）。
- ④ 已 commit + push 到 GitHub（origin 已带 token）。

### 本次关键判断
- 总判断：**中性**（规则回退 -0.10；模型综合当前融资宽松但脆弱项限定为中性）。
- 融资端宽松：SOFR-IORB -2.0bp、SOFR_VOLUME_IMPACT -1.8mn/day、信用利差收窄、VIX 15.57 低位回落、DTWEXBGS 走弱（离岸美元放松）、NFCI -0.504（负）、准备金 2.97T 充裕。
- **重要修正**：RRP 仅 4.48（$B，USD bn 单位）= 近耗尽，缓冲垫基本消失，非"健康"。RRP_FLOW +1.8bn（抽水），叠加 TGA +6.3bn，未来 TGA 补库/QT 冲击将直接打到准备金。此为结构性脆弱，非急性紧缩。
- 日元 carry：**中性偏高**（风险 label 用"中性"以符合 schema）。CFTC driver=short_building（空头 +7,162 口、多头 -1,826 口，当下仍在加仓、支撑美元），但 short_share 70.5% 处近 1 年 100% 分位、美日 10Y 利差仅 179bp，脆弱性高。
- 收益率：1Y/3Y/10Y = 3.95%/4.14%/4.48%，曲线正常陡峭、长端温和；但 DGS1/3/10 本次 previous=null，无周度方向（数据风险）。

### 数据风险
- **未来美债/T-bill 拍卖日程抓取失败**：TreasuryDirect `TA_WS/securities/search` 请求超时（read timeout=10s），`upcoming_auctions.status=error`、各列表 0 条。已标为 P1 数据风险，严禁当作零供给；RRP 近耗尽使其更关键。
- 提示：TreasuryDirect 接口偶发超时，可加重试/退避或在分析里显式标数据缺口（已做）。
