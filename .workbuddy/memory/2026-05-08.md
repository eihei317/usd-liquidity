# 2026-05-08

- 讨论美元流动性监测中的市场利率覆盖：当前三块（联储-银行、银行间、联储-货币基金）可作为短端资金市场核心框架，但后续应补充证券/债券资产定价层，包括 Treasury curve、T-bills、信用利差、融资/保证金利率等，用于解释从政策地板和短端资金价格到债券收益率、信用条件和股票估值的传导路径。
- 已将美元流动性监测升级为“短端资金市场 + 债券定价锚 + 信用/证券市场传导”框架：新增 DTB3、DFII10、BAMLC0A0CM、VIXCLS 等指标，补充 TBILL_CASH_YIELD、NOMINAL_10Y、REAL_10Y、IG_CHANGE、VIX_RISK 等衍生信号，传导链扩展为 Fed负债端水位 -> 政策锚/准备金边际 -> 银行间无抵押融资 -> 回购融资/抵押品链条 -> 债券定价锚/收益率曲线 -> 离岸美元 -> 信用市场/金融条件 -> 证券市场风险偏好。已运行脚本生成最新 output/latest 前端数据。

- TGA缓存机制修复：原 `read_tga_cache()`/`write_tga_cache()` 只存单条，导致 previous 始终为 NA。改为 `read_tga_history()`（读多条历史）/ `append_tga_history()`（追加写，按日期去重，保留最近90条）。`fetch_tga()` 改为拉90条API数据、把所有解析到的 (date, value) 写入缓存、再从缓存历史中找最近一个不同日期的值作为 previous。修复后 TGA previous=860.287bn (2026-05-05)，change=+2.47bn，状态 ok。
